<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Platform Material theme with no action bar — the entire UI is Compose.
        Using the platform theme (not AppCompat) keeps the appcompat package
        out of the dex entirely.
    -->
    <style name="Theme.YT4" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
